import React from "react";
import { StyleSheet, Text } from "react-native";

const CreateScreen = () => {
  return <Text style={styles.info}>Create Screen</Text>;
};
const styles = StyleSheet.create({});

export default CreateScreen;
